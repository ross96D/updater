@go.Package { name = "github.com/ross96D/updater/share/configuration" }
module updater.share.Configuration

import "package://pkg.pkl-lang.org/pkl-go/pkl.golang@0.6.0#/go.pkl"

class TaskSched {
    task_path: String
    app_path: String
}

class Application {
    owner: String
    repo: String
    host: String = "github.com"
    gitub_signature256: String
    github_auth_token: String

    asset_name: String
    task_sched_path: String
    app_path: String
    asset_verify: String? // TODO this need to be some kind of plugin because i dont know about some standart
}

/// The port to listen on.
port: UInt16

user_secret_key: String

user_jwt_expiry: Duration

apps: List<Application>

base_path: String?
